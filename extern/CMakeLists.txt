add_subdirectory(simdjson)

workspace_helpers_set_target_cxx_properties(simdjson)
workspace_helpers_set_target_output_directory_properties(simdjson)
workspace_helpers_set_target_pdb_properties(simdjson)

if(BUILD_TESTING)
  add_subdirectory(googletest)
  workspace_helpers_set_target_pdb_properties(gtest)
  workspace_helpers_set_target_pdb_properties(gtest_main)
  workspace_helpers_set_target_pdb_properties(gmock)
  workspace_helpers_set_target_pdb_properties(gmock_main)
endif()
